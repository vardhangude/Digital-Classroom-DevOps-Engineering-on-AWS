version: 0.2
phases:
  build:
    commands:
      - export BUCKET=lab4-sam-var-18
      - aws cloudformation package --template-file template.yaml --s3-bucket $BUCKET --output-template-file outputtemplate.yaml
artifacts:
  files:
    - outputtemplate.yaml